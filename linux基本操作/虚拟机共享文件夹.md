https://zhuanlan.zhihu.com/p/650638983

开启共享文件夹：
```text
sudo mount -t fuse.vmhgfs-fuse .host:/ /mnt/hgfs -o allow_other
```

附开机自动挂载：追加到/etc/fstab即可  
```
echo '.host:/  /mnt/hgfs fuse.vmhgfs-fuse allow_other,defaults 0 0' >> /etc/fstab
```