https://zhuanlan.zhihu.com/p/682574108
```
sudo apt install ros-${ROS_DISTRO}-ros2-control*
sudo apt-get install ros-${ROS_DISTRO}-robot-state-publisher ros-${ROS_DISTRO}-xacro

sudo apt install ros-${ROS_DISTRO}-backward-ros
sudo apt install ros-${ROS_DISTRO}-generate-parameter-library
```